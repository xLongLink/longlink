# Databases

The Databases section describes the control plane resources used to provision and manage database services for platform applications.

Use this area to document database operations, request models, returned metadata, and examples for provisioning, rotating, or inspecting database resources.

## Topics

- database provisioning
- connection metadata
- credentials and rotation flows
- database lifecycle operations
