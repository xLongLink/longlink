# Layout

LongLink does not have a separate layout DSL.
Page structure comes from nesting the `Page` root element, the `For` component, `p`, and the documented components.

Use the `For` component for repeated sections, `p` for simple text blocks, and `if="..."` on any element for conditional rendering.
