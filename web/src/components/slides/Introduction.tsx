/** Renders the opening split-arrow diagram. */
export function IntroductionSlide() {
    return (
        <img
            alt="A path splitting toward transparent and opaque boxes"
            className="h-full w-full object-contain"
            draggable={false}
            src="/images/paths.png"
        />
    );
}
