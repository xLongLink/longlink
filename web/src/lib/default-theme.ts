import { DEFAULT_RADIUS, getAstryxTheme } from './theme';

/** Default LongLink theme compiled to static CSS for the first paint. */
export const longlinkNeutral1Theme = getAstryxTheme('neutral', DEFAULT_RADIUS);
