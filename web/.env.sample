# Optional when the app is served from a different origin.
# Leave unset for local Vite dev, which proxies API requests to port 8000.
VITE_API_URL=

# Set from the release tag in CI, for example v1.2.3.
VERSION=
