[build-system]
requires = ['setuptools>=84.0.0', 'setuptools_scm[toml]>=10.2.1']
build-backend = 'setuptools.build_meta'

[project]
name = 'longlink-api'
dynamic = ['version']
description = 'LongLink backend API'
readme = 'README.md'
requires-python = '>=3.12'
dependencies = [
    'aioboto3>=15.5.0',
    'aiomysql>=0.3.2',
    'aiosmtplib>=5.1.2',
    'aiosqlite>=0.22.1',
    'alembic>=1.19.0',
    'asyncpg>=0.31.0',
    'botocore>=1.40.61',
    'cryptography>=50.0.0',
    'exoscale>=0.16.3',
    'fastapi>=0.141.1',
    'httpx2>=2.9.1',
    'pydantic>=2.13.4',
    'pydantic-settings>=2.15.0',
    'pyyaml>=6.0.3',
    'pwdlib[argon2]>=0.3.0,<1',
    'pyjwt>=2.10.1,<3',
    'python-slugify>=8.0.4',
    'psycopg[binary]>=3.3.4',
    'sqlalchemy>=2.0.51',
    'sqlalchemy-utils>=0.42.1',
    'sqlmodel>=0.0.39',
    'kr8s>=0.20.15',
    'longlink',
    'uvicorn>=0.52.1',
    'mjml>=0.12.0',
]

[tool.uv.sources]
longlink = { path = "../sdk", editable = true }
exoscale = { git = "https://github.com/xLongLink/python-exoscale", rev = "7a1ed139e8d535b150505506450dba72efa1314e" }

[project.optional-dependencies]
dev = [
    'ty>=0.0.69',
    'ruff>=0.16.2',
    'pytest>=9.0.3',
    'pytest-asyncio>=1.4.0',
    'docker>=7.2.0',
    'types-aiobotocore-s3==3.9.0',
]

[tool.setuptools_scm]
version_scheme = 'no-guess-dev'
local_scheme = 'no-local-version'
fallback_version = '0.0.0'

[tool.setuptools]
py-modules = ['main']

[tool.setuptools.packages.find]
include = ['scripts*', 'src*']

[tool.setuptools.package-data]
"src.kubernetes.templates" = ["application/*.yml", "platform/*.yml"]
"src.utils" = ["templates/*.mjml"]

[tool.ruff]
line-length = 140
target-version = "py312"

[tool.ruff.lint]
select = ["UP", "D300", "I"]

[tool.ruff.lint.per-file-ignores]
"src/database/models/*.py" = ["UP037", "UP045"]

[tool.ruff.lint.isort]
no-sections = true
length-sort = true

[tool.ty.environment]
extra-paths = ["../sdk"]

[tool.ty.src]
include = ["main.py", "scripts", "src", "alembic"]
# Keep SQLModel descriptor diagnostics out of the gate while checking callers.
exclude = ["src/.static", "tests", "src/database/models"]

[tool.ty.rules]
invalid-argument-type = "ignore"
no-matching-overload = "ignore"
unresolved-attribute = "ignore"

[tool.pytest.ini_options]
markers = [
    "integration: marks tests requiring container-backed infrastructure",
    "no_db: skips the API test database fixture for pure unit tests",
]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
