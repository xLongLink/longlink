apiVersion: v1
kind: Service
metadata:
  name: $name
  namespace: $namespace
  labels:
    managed-by: control-plane
    compute-role: application
spec:
  selector:
    app: $name
  ports:
    - port: 80
      targetPort: 80
