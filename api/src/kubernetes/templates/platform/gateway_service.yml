# Template for the standard public gateway LoadBalancer Service.
apiVersion: v1
kind: Service
metadata:
    name: longlink-gateway
    namespace: longlink-system
    labels:
        app: longlink-gateway
spec:
    type: LoadBalancer
    selector:
        app: longlink-gateway
    ports:
        - port: 443
          targetPort: gateway
