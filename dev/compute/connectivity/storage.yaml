apiVersion: v1
kind: Service
metadata:
    name: longlink-storage
    namespace: rook-ceph
spec:
    selector:
        app: rook-ceph-rgw
        rook_object_store: longlink
    ports:
        - name: https
          port: 9443
          targetPort: 443
          protocol: TCP
