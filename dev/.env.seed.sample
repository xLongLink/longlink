# Copy to dev/.env.seed to customize local sample settings.
# make up creates the local kubeconfig, certificates, and fixed infrastructure.
# The API administrator credentials and browser origin come from api/.env.

# Platform API
API_URL=http://127.0.0.1:8000
COMPUTE_TIMEOUT_SECONDS=300

# Sample release configuration
SAMPLE_ENVS={"REQUIRED":"development"}
