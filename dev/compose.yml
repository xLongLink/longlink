services:
  postgres:
    image: postgres:18-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: admin
    ports:
      - "15432:5432"

  registry:
    image: registry:3
    restart: unless-stopped
    ports:
      - "15000:5000"
