import { Button, Heading, Stack, Text } from "@ui";

export default function Reports() {
  return (
    <Stack gap={4}>
      <Heading level={1}>Reports</Heading>
      <Text>This page is transpiled from JSX in the LongLink browser runtime.</Text>
      <Button>Refresh</Button>
    </Stack>
  );
}
